Total cycle counts for plotting the loop rate distribution given by a minimum cycle and the relevant cycles. The file 
    `RGG_Length_Dists.npz`
is used by the notebook `random_geometric_graphs.ipynb`, where this data can also be recomputed.